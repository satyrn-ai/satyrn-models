{"code": "from string.templatelib import Template\n\nname = \"World\"\ntemplate = t\"Hello {name}\"\nassert isinstance(template, Template)", "description": "create a template string and check its type", "id": "create-a-template-string-and-check-its-type", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name}!\"\nassert template.strings == (\"Hello \", \"!\")\nassert len(template.interpolations) == 1", "description": "access the static string parts and interpolations of a template", "id": "access-the-static-string-parts-and-interpolations-of-a-template", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name}\"\nassert template.interpolations[0].value == \"World\"", "description": "read the evaluated value of an interpolation", "id": "read-the-evaluated-value-of-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name}\"\nassert template.interpolations[0].expression == \"name\"", "description": "read the source expression of an interpolation", "id": "read-the-source-expression-of-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name!r}\"\nassert template.interpolations[0].conversion == \"r\"", "description": "use a !r conversion in an interpolation", "id": "use-a-r-conversion-in-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "value = 42\ntemplate = t\"Value: {value:.2f}\"\nassert template.interpolations[0].format_spec == \".2f\"", "description": "use a format spec in an interpolation", "id": "use-a-format-spec-in-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "value = 42\nprecision = 2\ntemplate = t\"Value: {value:.{precision}f}\"\nassert template.interpolations[0].format_spec == \".2f\"", "description": "nest an interpolation inside a format spec", "id": "nest-an-interpolation-inside-a-format-spec", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name}!\"\ncontents = list(template)\nassert len(contents) == 3\nassert contents[0] == \"Hello \"\nassert contents[1].value == \"World\"\nassert contents[2] == \"!\"", "description": "iterate over the strings and interpolations of a template", "id": "iterate-over-the-strings-and-interpolations-of-a-template", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "first = \"Eat\"\nsecond = \"Red Leicester\"\ntemplate = t\"{first}{second}\"\nassert template.strings == (\"\", \"\", \"\")\nassert [i.value for i in template.interpolations] == [\"Eat\", \"Red Leicester\"]", "description": "note that adjacent interpolations leave empty strings in .strings", "id": "note-that-adjacent-interpolations-leave-empty-strings-in-strings", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Interpolation, Template\n\ndef lower_upper(template: Template) -> str:\n    \"\"\"Render static parts lowercased and interpolations uppercased.\"\"\"\n    parts: list[str] = []\n    for item in template:\n        if isinstance(item, Interpolation):\n            parts.append(str(item.value).upper())\n        else:\n            parts.append(item.lower())\n    return \"\".join(parts)\n\nname = \"world\"\nassert lower_upper(t\"HELLO {name}\") == \"hello WORLD\"", "description": "write a custom renderer that uppercases interpolations", "id": "write-a-custom-renderer-that-uppercases-interpolations", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Template\n\nname = \"World\"\ntemplate = t\"Hello \" + t\"{name}\"\nassert isinstance(template, Template)\nassert template.strings == (\"Hello \", \"\")\nassert template.values[0] == \"World\"", "description": "concatenate two templates with +", "id": "concatenate-two-templates-with", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello \" t\"{name}\"\nassert template.strings == (\"Hello \", \"\")\nassert template.values[0] == \"World\"", "description": "implicitly concatenate adjacent template literals", "id": "implicitly-concatenate-adjacent-template-literals", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Interpolation, Template\n\nname = \"World\"\nstatic = t\"Hello \" + Template(name)  # name becomes a static string part\ndynamic = t\"Hello \" + Template(Interpolation(name, \"name\"))  # name is interpolated\nassert static.strings == (\"Hello World\",)\nassert dynamic.interpolations[0].value == \"World\"", "description": "build a Template from a plain string or an Interpolation", "id": "build-a-template-from-a-plain-string-or-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {name=}\"\nassert template.strings[0] == \"Hello name=\"\nassert template.interpolations[0].value == \"World\"\nassert template.interpolations[0].conversion == \"r\"", "description": "use the {name=} debug syntax in a template", "id": "use-the-name-debug-syntax-in-a-template", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "trade = \"shrubberies\"\ntemplate = rt'Did you say \"{trade}\"?\\n'\nassert template.strings[0] == r'Did you say \"'\nassert template.strings[1] == r'\"?\\n' ", "description": "use a raw template string to keep backslashes literal", "id": "use-a-raw-template-string-to-keep-backslashes-literal", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Interpolation, Template\nfrom typing import Literal\n\ndef convert(value: object, conversion: Literal[\"a\", \"r\", \"s\"] | None) -> object:\n    if conversion == \"a\":\n        return ascii(value)\n    if conversion == \"r\":\n        return repr(value)\n    if conversion == \"s\":\n        return str(value)\n    return value\n\ndef f(template: Template) -> str:\n    parts: list[str] = []\n    for item in template:\n        match item:\n            case str() as s:\n                parts.append(s)\n            case Interpolation(value, _, conversion, format_spec):\n                parts.append(format(convert(value, conversion), format_spec))\n    return \"\".join(parts)\n\nname = \"World\"\nvalue = 42\nassert f(t\"Hello {name!r}, value: {value:.2f}\") == \"Hello 'World', value: 42.00\"", "description": "render a template like an f-string with a custom f() function", "id": "render-a-template-like-an-f-string-with-a-custom-f-function", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Interpolation, Template\n\ndef describe(template: Template) -> list[str]:\n    kinds: list[str] = []\n    for item in template:\n        match item:\n            case Interpolation(value=int()):\n                kinds.append(\"int\")\n            case Interpolation(value=str()):\n                kinds.append(\"str\")\n            case str():\n                pass\n    return kinds\n\nassert describe(t\"{1} and {'two'}\") == [\"int\", \"str\"]", "description": "match interpolations by value type with structural pattern matching", "id": "match-interpolations-by-value-type-with-structural-pattern-matching", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ntemplate = t\"Hello {(lambda: name)}\"\nassert callable(template.interpolations[0].value)\nassert template.interpolations[0].value() == \"World\"", "description": "interpolate a callable value", "id": "interpolate-a-callable-value", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Template\n\ndef reusable(name: str, question: str) -> Template:\n    return t\"Hello {name}, {question}?\"\n\ntemplate = reusable(\"King Arthur\", \"what is your quest\")\nassert template.values == (\"King Arthur\", \"what is your quest\")", "description": "return a reusable template from a function", "id": "return-a-reusable-template-from-a-function", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Template\n\nempty = t\"\"\nassert list(empty) == []\nassert empty.strings == (\"\",)\nassert empty.interpolations == ()\n\nvalue = \"hello\"\nsolo = t\"{value}\"\nassert solo.strings == (\"\", \"\")\nassert solo.interpolations[0].value == \"hello\"", "description": "inspect the structure of an empty template and one with only an interpolation", "id": "inspect-the-structure-of-an-empty-template-and-one-with-only-an-interpolation", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "value = 42\ntemplate = t\"{value=}\"\nassert template.strings[0] == \"value=\"\nassert template.interpolations[0].expression == \"value\"\nassert template.interpolations[0].conversion == \"r\"", "description": "expand the {value=} debug syntax explicitly", "id": "expand-the-value-debug-syntax-explicitly", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "from string.templatelib import Interpolation, Template\n\ndef sql(template: Template) -> tuple[str, list[object]]:\n    query: list[str] = []\n    params: list[object] = []\n    for item in template:\n        if isinstance(item, Interpolation):\n            query.append(\"?\")\n            params.append(item.value)\n        else:\n            query.append(item)\n    return \"\".join(query), params\n\nuser_id = 42\nstatement, params = sql(t\"SELECT * FROM users WHERE id = {user_id}\")\nassert statement == \"SELECT * FROM users WHERE id = ?\"\nassert params == [42]", "description": "build a parameterized SQL query from a template", "id": "build-a-parameterized-sql-query-from-a-template", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "import html as _html\nfrom string.templatelib import Interpolation, Template\n\ndef html(template: Template) -> str:\n    parts: list[str] = []\n    for item in template:\n        if isinstance(item, Interpolation):\n            parts.append(_html.escape(str(item.value)))\n        else:\n            parts.append(item)\n    return \"\".join(parts)\n\nevil = \"<script>alert('evil')</script>\"\nassert html(t\"<p>{evil}</p>\") == \"<p>&lt;script&gt;alert(&#x27;evil&#x27;)&lt;/script&gt;</p>\"", "description": "render HTML with automatic escaping of interpolated values", "id": "render-html-with-automatic-escaping-of-interpolated-values", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
{"code": "name = \"World\"\ncount = 3\ntemplate = t\"{name} has {count} items\"\nassert template.values == (\"World\", 3)", "description": "get all interpolated values with the .values property", "id": "get-all-interpolated-values-with-the-values-property", "provenance": "unverified", "reason": "Legacy hand-written example from the retired make_data.py. F-CONTAM source: these descriptions were byte-identical to prompts in the retired eval.py, so any pass rate measured against them was a memorization score. Seed material only -- never a corpus row, never a benchmark task."}
