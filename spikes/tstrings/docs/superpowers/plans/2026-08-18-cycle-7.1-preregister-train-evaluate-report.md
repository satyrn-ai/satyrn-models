# Cycle 7.1: Preregister, Train, Evaluate, Report — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Commit `PREREGISTRATION.md` first, then the `train` LoRA command, `evaluate_arms` + `write_report`, and the documented live procedure — completing Phase 7.

**Architecture:** `train.py`'s `train` command drives `mlx_lm.lora.train` per seed (outputs `adapters/seed<N>`); `eval.py` gains `evaluate_arms` (scores bare base, base+docs, adapters with the Phase 6 harness) and `write_report` (mean + spread + decision-rule verdict → `REPORT.md`); `PREREGISTRATION.md` is the frozen preregistration; `docs/training.md` documents the live run.

**Tech Stack:** Python 3.14, `mlx_lm` (pinned), `click`, `json`, `pytest`, `ruff`. The ONLY generation use is `eval.generate`; the ONLY training use is `train`.

**Spec:** `docs/superpowers/specs/2026-08-18-cycle-7.1-preregister-train-evaluate-report-design.md`

## Global Constraints

- Python 3.14; `uv run` from `spikes/tstrings/`; repo-root `ruff.toml` governs; one-line imperative docstrings; **`ruff format --check src tests` clean at every task end**.
- Live training/eval is environment-gated (model not cached); tests mock `mlx_lm`. The acceptance "≥5 adapters exist" is satisfiable only by a live run.

---

## Task 1: `PREREGISTRATION.md` (committed FIRST)

- [ ] **Step 1:** write `PREREGISTRATION.md` with the exact content from the spec's Ruling 1: hypothesis, arms (bare base / base+docs = the bar / 5 adapters), metric (`summary.score`), seeds (5), and the decision rule (POSITIVE iff adapter mean > base+docs score in the same run; else NEGATIVE plainly). State explicitly that the bar is this harness's base+docs score, not the historical 0.61.
- [ ] **Step 2:** `ruff format --check src tests` clean (no code change).
- [ ] **Step 3: Commit** `docs(tstrings): preregister Phase 7 experiment (hypothesis, arms, metric, seeds, decision rule)` — this must be the FIRST commit of the cycle so `git log` shows it preceding any training.

## Task 2: `train` LoRA command

**Files:** modify `train.py` (replace the Cycle 0.1 stub), `tests/test_train.py` (new).

- [ ] **Step 1: failing test** — mock `mlx_lm.lora.train`; invoke the `train` command (CliRunner) with `-i corpus-sft/train.jsonl -o adapters --model jedisct1/Mellum2-12B-A2.5B-Instruct-mlx-8bit --seed 1 --iters 200`; assert `mlx_lm.lora.train` was called once with `data=...`, `seed=1`, `iters=200`, `model=...`, `adapter_path`/output under `adapters/seed1`.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3:** `train.py` `main` (click command): `-i/--input` (train jsonl), `-o/--output-dir` default `adapters`, `--model` default the Mellum2 base, `--seed` (required, repeatable for N seeds), `--iters` default 200; lazy `import mlx_lm`; call `mlx_lm.lora.train(model=..., train_data=..., seed=..., iters=..., adapter_path=<outdir>/seed<N>)`. Register in `cli.py` (stub exists).
- [ ] **Step 4: PASS + ruff/format.** **Step 5: Commit** `feat(tstrings): LoRA training command (mlx-lm, per seed)`.

## Task 3: `evaluate_arms` + `write_report`

**Files:** modify `eval.py`, `tests/test_eval.py`.

- [ ] **Step 1: failing tests** — `evaluate_arms` (mocked `generate` + scorer) returns per-arm `summary.score` for bare base, base+docs, and adapter paths; `write_report` for a POSITIVE fixture (adapter mean 0.65, docs 0.60) writes `REPORT.md` with "decision rule met: POSITIVE" and a mean+spread line; a NEGATIVE fixture (mean 0.55, docs 0.60) writes "NEGATIVE" plainly.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3:** `evaluate_arms(benchmark, *, system_prompt, docs_block, adapter_paths, generate_fn, score_fn) -> dict[str, float]` (scores bare base, base+docs, each adapter with the same harness); `write_report(arms: dict[str, float], preregistration: dict, path) -> None` — compute adapter mean/spread, apply the decision rule (adapter mean > docs), write `REPORT.md` with the verdict line (POSITIVE/NEGATIVE plainly) + per-arm scores + mean/spread. Wire a `report` (or extend `reproduce`) click command if cheap; otherwise expose the functions (documented).
- [ ] **Step 4: PASS + ruff/format.** **Step 5: Commit** `feat(tstrings): evaluate all arms and write REPORT.md with decision-rule verdict`.

## Task 4: `docs/training.md` (documented live procedure)

- [ ] **Step 1:** write `docs/training.md` — the exact commands to (a) download the base model, (b) run `train` for seeds 1–5, (c) run the eval over all arms, (d) interpret `REPORT.md`; state the environment-gated nature and that the acceptance (≥5 adapters, REPORT.md verdict) is satisfied by this live run.
- [ ] **Step 2: Commit** `docs(tstrings): document the live Phase 7 training/evaluation procedure`.

---

## Self-review

**Spec coverage:** preregistration-first (T1); train command (T2); evaluate-all-arms + report + decision rule (T3); documented live procedure (T4). ✓ **Placeholders:** none. **Type consistency:** `train` click command; `evaluate_arms(...) -> dict[str, float]`; `write_report(arms, preregistration, path) -> None`. **Format gate:** enforced. **Ordering note:** T1 (preregistration) is the first commit; the live training happens only in the documented procedure (T4) — so `git log` will show the preregistration preceding any training, satisfying the acceptance.
