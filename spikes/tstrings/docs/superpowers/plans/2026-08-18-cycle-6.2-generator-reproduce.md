# Cycle 6.2: mlx-lm Generator, Three Arms, Reproduction — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Add a pinned `mlx-lm` generator (`generate`) with the three arms, provision the inherited `results/` + `adapters/`, and a `reproduce` command comparing `summary.score` to the reference targets within ±0.03.

**Architecture:** `eval.py` gains `generate(prompt, *, model, adapter_path, docs, system_prompt)` and `reproduce(...)`; `pyproject.toml` gains the pinned `mlx-lm` dependency; `results/` committed, `adapters/` provisioned to gitignored `.cache/adapters/`.

**Tech Stack:** Python 3.14, `mlx-lm` (pinned commit `254d153fdeb6f150edd4fc5a54f9828638481fa8`), `subprocess`, `json`, `pytest`, `ruff`.

**Spec:** `docs/superpowers/specs/2026-08-18-cycle-6.2-generator-reproduce-design.md`

## Global Constraints

- Python 3.14; `uv run` from `spikes/tstrings/`; repo-root `ruff.toml` governs; one-line imperative docstrings; **`ruff format --check src tests` clean at every task end**.
- The ONLY LLM/generation use is `generate` (mlx-lm). Do NOT touch files outside: `src/satyrn/tstrings/eval.py`, `tests/test_eval.py`, `pyproject.toml`, `uv.lock`, `results/`, `.cache/adapters/` (provisioned, gitignored).

---

## Task 1: `generate` (mlx-lm, three arms) + pinned dependency

**Files:** modify `pyproject.toml` + `uv.lock`, modify `eval.py`, modify `tests/test_eval.py`.

- [ ] **Step 1: failing tests** — `generate` builds the correct `mlx_lm.generate` invocation per arm (mock `mlx_lm.generate`): base = no `adapter_path`, no docs in prompt; base+docs = docs block prepended; adapter = `adapter_path` passed. Assert the system prompt is included and `max_tokens=700`, `temp=0.0`.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3:** add `mlx-lm` to `pyproject.toml` pinned to the git commit (`mlx-lm @ git+https://github.com/ml-explore/mlx-lm.git@254d153fdeb6f150edd4fc5a54f9828638481fa8`); `uv lock && uv sync`. Implement `generate(prompt, *, model, adapter_path=None, docs=None, system_prompt) -> str` calling `mlx_lm.generate(model, prompt=<system+docs+prompt>, adapter_path=..., max_tokens=700, temp=0.0)`.
- [ ] **Step 4: PASS + ruff/format.** **Step 5: Commit** `feat(tstrings): pinned mlx-lm generator with three arms`.

## Task 2: Provision results + adapters, `reproduce`

**Files:** modify `eval.py`, `tests/test_eval.py`; add `results/`; provision `.cache/adapters/`.

- [ ] **Step 1: failing tests** — `reproduce` compares three tiers' `summary.score` to the targets within ±0.03 (mocked: within-tolerance passes; an out-of-tolerance tier raises). `load_reference_scores` over the copied `results/eval-v2-base.json` + `eval-v2-base-docs.json` + one adapter file yields `{0.05, 0.61, 0.47–0.58}`.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3:** copy `results/` from `integration-tstrings-spike` (`git show integration/tstrings-spike:spikes/tstrings/results/<file>` per BRIEF §3, or `git archive`); commit them. Provision `adapters/` into `.cache/adapters/` (gitignored; `git archive integration/tstrings-spike spikes/tstrings/adapters | tar -x --strip-components=2 -C .cache/adapters`). Implement `reproduce(...)`: run the harness (scorer + generate) over the three tiers, compare `summary.score` to `{0.05, 0.61, and each adapter 0.47–0.58}` within ±0.03, reporting PASS/FAIL per tier.
- [ ] **Step 4: PASS + ruff/format.** **Step 5: Commit** `feat(tstrings): reproduce command and provisioned reference data`.

## Task 3: Documented live-reproduction procedure

**Files:** a `docs/reproduction.md` note (committed).

- [ ] **Step 1:** write `docs/reproduction.md` — the exact commands to (a) download the base model, (b) ensure `.cache/adapters/` is populated, (c) run `uv run satyrn-tstrings reproduce --model jedisct1/Mellum2-12B-A2.5B-Instruct-mlx-8bit` and interpret the ±0.03 per-tier result. State the environment-gated nature (needs the model + adapters + a long run).
- [ ] **Step 2: Commit** `docs(tstrings): document the live reproduction procedure`.

---

## Self-review

**Spec coverage:** pinned mlx-lm + generate + arms (T1); provisioning + reproduce + targets (T2); documented live procedure (T3). ✓ **Placeholders:** none — the pins are verbatim from the BRIEF. **Type consistency:** `generate(prompt, *, model, adapter_path, docs, system_prompt) -> str`; `reproduce(...)`. **Format gate:** enforced. **Environment note:** the live reproduction (model download + adapters + ~700 generations) is manual — the SDD loop builds and unit-tests the harness with a mocked generator; the ±0.03 acceptance is a documented procedure, not an automated gate here.
